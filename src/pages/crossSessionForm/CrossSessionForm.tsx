import React, { useEffect, useState } from 'react';
import moment from 'moment';
import { useDispatch } from 'react-redux';

interface ICrossSessionForm {}

export const CrossSessionForm: React.FC = (props: ICrossSessionForm) => {
  return;
};
